// Asignar un número a la variable
let numero = 10; // Puedes cambiar este valor por cualquier número entero

// Verificar si el número es par o impar
if (numero % 2 === 0) {
    console.log("El número es par.");
} else {
    console.log("El número es impar.");
}